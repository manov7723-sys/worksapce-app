server {
    listen 80;
    server_name htcai.local;

    location / {
        proxy_pass         http://app:80;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-Host $host;

        # Rewrite: htcai.local/ → app:3000/a/htcai
        rewrite ^/(.*)$ /a/htcai/$1 break;
    }
}

server {
    listen 80;
    server_name tecneuralai.local;

    location / {
        proxy_pass         http://app:80;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-Host $host;

        # Rewrite: tecneuralai.local/ → app:3000/a/tecneuralai
        rewrite ^/(.*)$ /a/tecneuralai/$1 break;
    }
}